# getrandom 0.3 (vía lopdf → rand) exige un backend explícito en
# wasm32-unknown-unknown. La feature `wasm_js` sola no basta: también
# necesita este cfg. Ver https://docs.rs/getrandom/0.3/#webassembly-support
[target.wasm32-unknown-unknown]
rustflags = ['--cfg', 'getrandom_backend="wasm_js"']
